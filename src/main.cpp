/** @file main.cpp
 *
 * @brief main/debug executable for Assignment Analysis of
 *   Algorithms
 *
 * @author Derek Harter
 * @note   class: COSC 2336, Fall 2021
 * @note   ide  : VSCode Server 3.9.3, Gnu Development Tools
 * @note   assg : Assignment Sorting and Searching
 * @date   October 1, 2021
 *
 * Implement algorithms with varying time complexities and compare
 * their performance.
 *
 * This file is a stub for a main() function so that we can build a
 * version suitable for running in the debugger.
 */
#include "libfibonacci.hpp"
#include <iostream>
using namespace std;

/** main entry point
 * Main entry point for debugging functions.
 *
 * @param argc The command line argument count, the number of arguments
 *   provided by user on the command line.
 * @param argv An array of char* old style c-strings.  Each argv[x]
 *   that is passed in holds one of the command line arguments provided
 *   by the user to the program when started.
 *
 * @returns int Returns 0 to indicate successfull completion of program,
 *   and a non-zero value to indicate an error code.
 */
int main(int argc, char** argv)
{
  // do something here to test/debug the assignment code
  cout << "Hello Assignment Analysis of Algorithms" << endl;

  // return 0 to indicate successful completion of program
  return 0;
}
